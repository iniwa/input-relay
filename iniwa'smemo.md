# **__実装が完了したものはチェックマークを付けてください__**
# serenaでインデックスしてから作業を開始してください

## 実装予定
 - [x] マウスクリックの表示 → キーボードタブで「+ マウス左/右/中」ボタンから追加可能。sender側も mouse_left/right/middle イベント送信済
 - [x] レイアウトプリセットの更新（上書き保存） → `overwritePreset()` 関数、「上書き保存」ボタン実装済
 - [x] 全設定まとめて行･列の移動 → `applyOffset()` 関数、3モード全てに対応済
 - [x] 順序について、ドラッグ&ドロップで移動できるようにしてほしい（既存のボタン形式は削除） → `initRowDrag()` でテーブル行D&D実装済
 - [x] 行･列･横幅の設定に小数点を設定できるようにしてほしい → 全input要素に `step="0.1"`、`parseFloat()` 使用
 - [x] レバーレス･コントローラモードにもプリセットの登録 → 3タブ全てにプリセットUI実装済
 - [x] コントローラにて、PS5コントローラ･XBOXコントローラのプリセットを作ってほしい → `presets.json` に「PS5 Controller」「Xbox Controller」存在
 - [x] ラベル内での改行 → `\n` → `<br>` 変換を `pvSetLabel()` と `setLabelContent()` で実装済
 - [x] `sender`側を開いた際はweb GUIを開かなくて良い → `start_sender.bat` にブラウザ起動なし。senderはCLIのみ
 - [x] コントローラ設定、アナログスティック設定の「方向ソース」ってなに？ → config_gui.html に説明テキスト表示済: 「方向入力に使うソースを選択: leftStick=左スティック, rightStick=右スティック, hat=十字キー(D-pad)」
 - [x] コントローラ設定のスティックの位置を調整できない → overlay.htmlのコントローラレイアウトをCSS Gridに変更し、stickPositionsのrow/colが反映されるよう修正済
 - [x] L2/R2のアナログ入力（押し込み加減）は取得できる？ → axis 4/5 で送受信、トリガーバーで可視化済
 - [x] 履歴の表示は違うwebソースへ出力するようにしてほしい（レイアウトはOBSで調整） → `overlay.html?mode=input` でキー表示のみ、`overlay.html?mode=history` で履歴のみ。config GUIの入力履歴タブに使い方の説明を追加済
 - [x] 履歴表示の「アイドルタイムアウト」とはどういう設定ですか？ここの時間が過ぎたら履歴からも消えるようにして欲しいです（コントローラに触らずにいるとこの時間経過後に画面から履歴表示が消えるようなイメージ）（また触ると履歴が更新されて出てくる） → 要望通りの動作: idleTimeoutMs経過後にフェード → +2秒で完全非表示 → 入力で復活。config GUIに説明文を追加済
 - [x] レイアウトの切り替えはサブPCのweb GUIで行うようにしてください → プレビュー欄に「KB切替/LV切替/CT切替」ボタン追加、POST /api/mode-switch でオーバーレイをリアルタイム切替

## 不具合
 - [x] `Shift+1`といったShift+数字キーが反応しない、数字キーを押しながらShiftを押すと数字キーが押されっぱなしになる → key_to_str()でvk(仮想キーコード)を使用し、修飾キー状態に依存しない一貫したキー名を返すよう修正済
 - [x] Ctrlが反応しない → 同上。vkコードによりCtrl押下時の制御文字(例: \x03)ではなく元のキー名を返すよう修正。右修飾キー(shift_r, ctrl_r, alt_r)も左と同じ名前に統一済
 - [ ] 動作が非常に不安定（localhostでテスト時）。実際の環境で試す → asyncioイベントループ初期化修正済(e6fb5fe)。キー正規化修正も安定性に寄与。2PC構成での実環境テストで再確認が必要
