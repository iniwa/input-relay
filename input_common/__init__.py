"""Shared implementation used by both sender/ and receiver/ (standalone mode).

Not a distributable package — repo-root import only, see the sys.path
bootstrap in sender/input_sender.py, sender/gamepad.py, and
receiver/standalone_capture.py.
"""
